class Model
{
    static all()
    {
        // Get all elements
    }

    static find(ids)
    {
        // Find one or more elements
    }

    static destroy(ids)
    {
        // Delete one or more elements
    }
}

module.exports = Model;
